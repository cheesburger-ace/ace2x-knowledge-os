# Contributing

1. Create a branch from `main`.
2. Make source changes in `src/main.ts`.
3. Run `npm run build`.
4. Test the generated plugin in an Obsidian test vault.
5. Update release notes when behavior changes.
6. Submit a pull request.

Do not edit generated `main.js` as the primary source. Changes should originate in `src/main.ts`.
